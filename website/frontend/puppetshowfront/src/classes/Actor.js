class Actor {
  constructor(name, hash, base_user, animations) {
    this.name = name;
    this.hash = hash;
    this.base_user = base_user;
    this.animations = animations;
  }
}

export default Actor;
